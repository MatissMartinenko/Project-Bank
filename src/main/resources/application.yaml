spring:
  main:
    banner-mode: off

  datasource:
    url: jdbc:mysql://localhost:3306/project_bank
    username: root
    password: 26673349
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  liquibase:
      change-log: classpath:/db/changelog/changelog-master.xml